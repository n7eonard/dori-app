<div id="div-heures"  style="background-image: url(<%= image_path 'background-form.png'%>)">

<h2 class ="form-title">Vos créneaux</h2>

<div class="heures">
<%= simple_form_for @program do |f| %>
  <% @program.cards_builder.keys.each do |day| %>
    <% if ["lundi","mardi","mercredi","jeudi","vendredi","samedi","dimanche"].include? day %>
    <div class="jours">
    <%= day.capitalize %>
    </div>
      <%= f.input "#{day}_start", as: :text, input_html: { value: "" }, label: 'Début' %>
      <%= f.input "#{day}_end", as: :text, input_html: { value: "" }, label: 'Fin' %>
    <% end %>
<% end %>
<%= image_tag "Path 2.png", id: "fleche-bas2" %>
</div>
</div>

<div id="div-adresses" class="hidden"  style="background-image: url(<%= image_path 'background-form.png'%>)">

<h2 class ="form-title">Vos piscines :</h2>
<p id="text-address">Choisissez l'adresse à proximité de laquelle vous souhaitez nager</p>
<% @program.cards_builder.keys.each do |day| %>
    <% if ["lundi","mardi","mercredi","jeudi","vendredi","samedi","dimanche"].include? day %>
      <%= f.input "#{day}_address", as: :text, input_html: { value: "" }, label: day %>
  <% end %>
   <% end %>
<%= image_submit_tag "Path 2.png", id: "arrow_last" %>
<% end %>

</div>
